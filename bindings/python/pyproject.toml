[project]
name = "iosuite"
version = "0.1.0"
description = "Python bindings for iosuite"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
